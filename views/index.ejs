<%- include('layout', { body: `
<section class="hero">
  <img src="/images/Shiva.jpeg" alt="Mahadev">
  <p>Explore the divine journey of Mahadev.</p>
</section>

<section class="quotes">
  <h2>Quotes</h2>
  <ul>
    <% quotes.forEach(quote => { %>
      <li>"<%= quote %>"</li>
    <% }) %>
  </ul>
</section>
` }) %>
